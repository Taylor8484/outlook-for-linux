{
  "$generatedBy": "scripts/generateConfigDocs.js",
  "source": "app/config/options.js",
  "optionCount": 59,
  "options": [
    {
      "name": "appIcon",
      "type": "string",
      "default": "",
      "description": "Custom app icon (PNG) for the tray, the window icon on Windows and Linux, and the dock on macOS. Also settable from the App Icon menu",
      "applyMode": "live",
      "deprecated": "use tray.icon instead"
    },
    {
      "name": "appIconType",
      "type": "string",
      "default": "default",
      "description": "Type of tray icon to be used",
      "applyMode": "restart",
      "deprecated": "use tray.iconType instead"
    },
    {
      "name": "appTitle",
      "type": "string",
      "default": "Microsoft Outlook",
      "description": "A text to be suffixed with page title",
      "applyMode": "restart",
      "deprecated": "use app.title instead"
    },
    {
      "name": "alwaysOnTop",
      "type": "boolean",
      "default": true,
      "description": "Keep the pop-out window always on top of other windows.",
      "applyMode": "restart",
      "deprecated": "use window.alwaysOnTop instead"
    },
    {
      "name": "authServerWhitelist",
      "type": "string",
      "default": "*",
      "description": "Set auth-server-whitelist value",
      "applyMode": "restart"
    },
    {
      "name": "chromeUserAgent",
      "type": "string",
      "default": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/<version> Safari/537.36",
      "description": "Google Chrome User Agent",
      "applyMode": "restart",
      "deprecated": "use platform.chromeUserAgent instead"
    },
    {
      "name": "customCACertsFingerprints",
      "type": "array",
      "default": [],
      "description": "Array of custom CA Certs Fingerprints to allow SSL unrecognized signer or self signed certificate",
      "applyMode": "restart"
    },
    {
      "name": "customCSSLocation",
      "type": "string",
      "default": "",
      "description": "custom CSS styles file location",
      "applyMode": "restart",
      "deprecated": "use appearance.cssLocation instead"
    },
    {
      "name": "class",
      "type": "string",
      "default": null,
      "description": "A custom value for the WM_CLASS property",
      "applyMode": "restart",
      "deprecated": "use window.class instead"
    },
    {
      "name": "cacheManagement",
      "type": "object",
      "default": {
        "enabled": false,
        "maxCacheSizeMB": 600,
        "cacheCheckIntervalMs": 3600000
      },
      "description": "Cache management configuration to prevent daily logout issues",
      "applyMode": "restart",
      "fields": [
        {
          "path": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Enable automatic cache management to prevent daily logout issues."
        },
        {
          "path": "maxCacheSizeMB",
          "type": "number",
          "default": 600,
          "description": "Maximum cache size in MB before cleanup runs."
        },
        {
          "path": "cacheCheckIntervalMs",
          "type": "number",
          "default": 3600000,
          "description": "How often the cache size is checked, in milliseconds."
        }
      ]
    },
    {
      "name": "clearStorageData",
      "type": "boolean",
      "default": null,
      "description": "Flag to clear storage data. Expects an object of the type https://www.electronjs.org/docs/latest/api/session#sesclearstoragedataoptions",
      "applyMode": "restart",
      "deprecated": "use storage.clearData instead"
    },
    {
      "name": "clientCertPath",
      "type": "string",
      "default": "",
      "description": "Custom Client Certs for corporate authentication (certificate must be in pkcs12 format)",
      "applyMode": "restart"
    },
    {
      "name": "clientCertPassword",
      "type": "string",
      "default": "",
      "description": "Custom Client Certs password for corporate authentication (certificate must be in pkcs12 format)",
      "applyMode": "restart"
    },
    {
      "name": "closeAppOnCross",
      "type": "boolean",
      "default": false,
      "description": "Close the app when clicking the close (X) cross",
      "applyMode": "restart",
      "deprecated": "use window.closeOnCross instead"
    },
    {
      "name": "defaultNotificationUrgency",
      "type": "string",
      "default": "normal",
      "description": "Default urgency for new notifications (low/normal/critical)",
      "applyMode": "live"
    },
    {
      "name": "defaultURLHandler",
      "type": "string",
      "default": "",
      "description": "Default application to be used to open the HTTP URLs",
      "applyMode": "restart",
      "deprecated": "use urlHandling.defaultHandler instead"
    },
    {
      "name": "disableGpu",
      "type": "boolean",
      "default": false,
      "description": "A flag to disable GPU and hardware acceleration (can be useful if the window remains blank)",
      "applyMode": "restart",
      "deprecated": "use performance.disableGpu instead"
    },
    {
      "name": "download",
      "type": "object",
      "default": {
        "enabled": false,
        "notifyOnDownloadComplete": true,
        "showProgressBar": true,
        "showTitlePrefix": true,
        "saveDirectory": "",
        "alwaysAskWhereToSave": false,
        "openWhenDone": false
      },
      "description": "Download manager configuration. enabled: master switch for the entire feature, defaults to false while the feature is in early development — set true to opt in. notifyOnDownloadComplete: show a system notification when a file download finishes (click opens the containing folder). showProgressBar: drive the taskbar progress bar and KDE JobView / Unity LauncherEntry signals while downloads are in flight. showTitlePrefix: also prefix the window title with [N%] as a portable fallback for environments where the other progress signals aren't rendered; set to false to keep the title untouched when KDE / Ubuntu already show progress elsewhere. saveDirectory: absolute path to always save allowed downloads into without prompting (empty string uses the OS default download directory). alwaysAskWhereToSave: show the native Save As dialog for every download (takes precedence over saveDirectory). openWhenDone: open each completed download in the OS default handler. When a download is interrupted by a Microsoft 365 / SharePoint / tenant policy the failure notification explains the likely cause and clicking it opens the file's link in your browser. All sub-flags only take effect when enabled is true.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Master switch for the download feedback feature; sub-flags only take effect when this is true."
        },
        {
          "path": "notifyOnDownloadComplete",
          "type": "boolean",
          "default": true,
          "description": "Show a system notification when a file download finishes (click opens the containing folder)."
        },
        {
          "path": "showProgressBar",
          "type": "boolean",
          "default": true,
          "description": "Drive the taskbar progress bar and KDE JobView / Unity LauncherEntry signals while downloads are in flight."
        },
        {
          "path": "showTitlePrefix",
          "type": "boolean",
          "default": true,
          "description": "Prefix the main window title with download progress as a portable fallback where other progress signals are not rendered."
        }
      ]
    },
    {
      "name": "disableNotifications",
      "type": "boolean",
      "default": false,
      "description": "A flag to disable all notifications",
      "applyMode": "live"
    },
    {
      "name": "disableNotificationSound",
      "type": "boolean",
      "default": false,
      "description": "Disable notification sound",
      "applyMode": "live"
    },
    {
      "name": "disableNotificationWindowFlash",
      "type": "boolean",
      "default": false,
      "description": "A flag indicates whether to disable window flashing when there is a notification",
      "applyMode": "live"
    },
    {
      "name": "notifications",
      "type": "object",
      "default": {
        "timeoutType": "default",
        "electron": {
          "clickAction": "show"
        }
      },
      "description": "Notification behaviour. timeoutType: how long notifications stay in the system notification center (Linux/Windows only). Choices: `default` (auto-clear per system policy) or `never` (persist until the user dismisses, useful on GNOME and other desktops that auto-remove notifications). Mirrors Electron's Notification timeoutType. May not be honoured by every notification daemon. electron.clickAction: what clicking a notification does when notificationMethod is `electron`. Choices: `show` (reveal the window and let Outlook open the item the notification came from, the default), `restore` (also un-minimise and focus, which helps on GNOME where a plain show does not raise the window) or `none` (do nothing, and do not open the item either).",
      "applyMode": "restart",
      "fields": [
        {
          "path": "timeoutType",
          "type": "string",
          "default": "default",
          "description": "How long notifications stay in the system notification center (Linux/Windows only); may not be honoured by every notification daemon.",
          "choices": [
            "default",
            "never"
          ]
        },
        {
          "path": "electron.clickAction",
          "type": "string",
          "default": "show",
          "description": "What clicking an Electron notification does (notificationMethod `electron` only): `show` reveals the window and lets Outlook open the item the notification came from (default), `restore` also un-minimises and focuses it (helps on GNOME), `none` does nothing at all.",
          "choices": [
            "show",
            "restore",
            "none"
          ]
        }
      ]
    },
    {
      "name": "disableBadgeCount",
      "type": "boolean",
      "default": false,
      "description": "A flag indicates whether to disable the badge counter on the taskbar/dock icon",
      "applyMode": "live"
    },
    {
      "name": "disableGlobalShortcuts",
      "type": "array",
      "default": [],
      "description": "Array of global shortcuts to disable while the app is in focus. See https://www.electronjs.org/docs/latest/api/accelerator for available accelerators to use",
      "applyMode": "restart",
      "deprecated": "use shortcuts.disableWhileFocused instead"
    },
    {
      "name": "globalShortcuts",
      "type": "array",
      "default": [],
      "description": "Global keyboard shortcuts that work system-wide. Disabled by default (opt-in). See configuration docs for details and limitations",
      "applyMode": "restart",
      "deprecated": "use shortcuts.global instead"
    },
    {
      "name": "electronCLIFlags",
      "type": "array",
      "default": [],
      "description": "Electron CLI flags",
      "applyMode": "restart",
      "deprecated": "use performance.electronCLIFlags instead"
    },
    {
      "name": "emulateWinChromiumPlatform",
      "type": "boolean",
      "default": false,
      "description": "Use windows platform information in chromium. This is helpful if MFA app does not support Linux.",
      "applyMode": "restart",
      "deprecated": "use platform.emulateWindowsChromium instead"
    },
    {
      "name": "frame",
      "type": "boolean",
      "default": true,
      "description": "Specify false to create a Frameless Window. Default is true",
      "applyMode": "restart",
      "deprecated": "use window.frame instead"
    },
    {
      "name": "logConfig",
      "type": "object",
      "default": {
        "transports": {
          "console": {
            "level": "info"
          },
          "file": {
            "level": false
          }
        }
      },
      "description": "Electron-log configuration. See logger.js for configurable values. To disable it provide a Falsy value.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "transports.console.level",
          "type": "string|boolean",
          "default": "info",
          "description": "electron-log level for the console transport (error/warn/info/verbose/debug/silly); set to false to disable console logging."
        },
        {
          "path": "transports.file.level",
          "type": "string|boolean",
          "default": false,
          "description": "electron-log level for the file transport (error/warn/info/verbose/debug/silly); false (the default) disables file logging."
        }
      ]
    },
    {
      "name": "menubar",
      "type": "string",
      "default": "auto",
      "description": "A value controls the menu bar behaviour",
      "applyMode": "restart",
      "deprecated": "use window.menubar instead"
    },
    {
      "name": "minimized",
      "type": "boolean",
      "default": false,
      "description": "Start the application minimized",
      "applyMode": "restart",
      "deprecated": "use window.minimized instead"
    },
    {
      "name": "minimizeOnClose",
      "type": "boolean",
      "default": false,
      "description": "Minimize the window when clicking the close (X) cross instead of hiding it to the tray (ignored when closeAppOnCross is true)",
      "applyMode": "restart",
      "deprecated": "use window.minimizeOnClose instead"
    },
    {
      "name": "notificationMethod",
      "type": "string",
      "default": "web",
      "description": "Notification method to be used by the application (web/electron/custom)",
      "applyMode": "restart"
    },
    {
      "name": "customNotification",
      "type": "object",
      "default": {
        "toastDuration": 5000
      },
      "description": "Custom in-app notification system configuration",
      "applyMode": "restart",
      "fields": [
        {
          "path": "toastDuration",
          "type": "number",
          "default": 5000,
          "description": "Time in milliseconds before a custom toast notification auto-dismisses."
        }
      ]
    },
    {
      "name": "partition",
      "type": "string",
      "default": "persist:outlook-4-linux",
      "description": "BrowserWindow webpreferences partition",
      "applyMode": "restart",
      "deprecated": "use app.partition instead"
    },
    {
      "name": "proxyServer",
      "type": "string",
      "default": null,
      "description": "Proxy Server with format address:port",
      "applyMode": "restart"
    },
    {
      "name": "network",
      "type": "object",
      "default": {
        "disableQuic": true
      },
      "description": "Network configuration. disableQuic: Append Chromium's --disable-quic switch at startup. Defaults to true to work around issue #2518 (concurrent SharePoint downloads abort with ERR_QUIC_PROTOCOL_ERROR on the shared QUIC session). Set to false to re-enable QUIC if a future Chromium release fixes the underlying transport bug.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "disableQuic",
          "type": "boolean",
          "default": true,
          "description": "Append Chromium's --disable-quic switch at startup to work around QUIC protocol errors (issue #2518)."
        }
      ]
    },
    {
      "name": "spellCheckerLanguages",
      "type": "array",
      "default": [],
      "description": "Array of languages to use with Electron's spell checker (experimental)",
      "applyMode": "restart",
      "deprecated": "use platform.spellCheckerLanguages instead"
    },
    {
      "name": "ssoBasicAuthUser",
      "type": "string",
      "default": "",
      "description": "User to use for SSO basic auth.",
      "applyMode": "restart"
    },
    {
      "name": "ssoBasicAuthPasswordCommand",
      "type": "string",
      "default": "",
      "description": "Command to execute to retrieve password for SSO basic auth.",
      "applyMode": "restart"
    },
    {
      "name": "trayIconEnabled",
      "type": "boolean",
      "default": true,
      "description": "Enable tray icon",
      "applyMode": "restart",
      "deprecated": "use tray.enabled instead"
    },
    {
      "name": "url",
      "type": "string",
      "default": "https://outlook.office.com/mail/",
      "description": "Microsoft Outlook URL",
      "applyMode": "restart",
      "deprecated": "use app.url instead"
    },
    {
      "name": "useMutationTitleLogic",
      "type": "boolean",
      "default": true,
      "description": "Use MutationObserver to update counter from title",
      "applyMode": "restart",
      "deprecated": "use tray.useMutationTitleLogic instead"
    },
    {
      "name": "watchConfigFile",
      "type": "boolean",
      "default": false,
      "description": "Watch for changes in the config file and reload the app",
      "applyMode": "restart",
      "deprecated": "use development.watchConfigFile instead"
    },
    {
      "name": "webDebug",
      "type": "boolean",
      "default": false,
      "description": "Enable debug at start",
      "applyMode": "restart",
      "deprecated": "use development.webDebug instead"
    },
    {
      "name": "media",
      "type": "object",
      "default": {
        "macPerformanceMode": true
      },
      "description": "Media and rendering settings. macPerformanceMode: on macOS, force-enable native hardware/rendering optimizations (Metal ANGLE, GPU rasterization) at startup; defaults to true, set false to opt out without disabling the GPU entirely.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "macPerformanceMode",
          "type": "boolean",
          "default": true,
          "description": "On macOS, force-enable native hardware/rendering optimizations (Metal ANGLE, GPU rasterization) at startup; set false to opt out without disabling the GPU entirely."
        }
      ]
    },
    {
      "name": "auth",
      "type": "object",
      "default": {
        "intune": {
          "enabled": false,
          "user": ""
        },
        "webauthn": {
          "enabled": false,
          "debug": false,
          "extraOrigins": []
        },
        "reauthRecovery": {
          "enabled": false
        },
        "clientCertificate": {
          "pinDialog": {
            "enabled": false
          }
        },
        "webLogin": {
          "user": "",
          "passwordCommand": "",
          "extraHosts": [],
          "autoSubmit": false,
          "verifyMethod": ""
        },
        "keepMsalCacheEncryptionCookie": {
          "enabled": true,
          "days": 400
        }
      },
      "description": "Authentication configuration. auth.webauthn.enabled turns on hardware security key support on Linux (requires fido2-tools). auth.webauthn.debug enables verbose diagnostic logs, intended for beta testers only. auth.webauthn.extraOrigins adds exact https sign-in origins allowed to use hardware keys, for federated tenants whose FIDO2 prompt is served off the Microsoft login hosts. auth.reauthRecovery.enabled opts into in-app re-authentication recovery and is off by default. When on, a reliable MSAL InteractionRequired signal from Outlook or the Microsoft login pages automatically clears stale auth state and reloads to force a fresh interactive login, and a Microsoft login popup opened within an hour of such a signal is intercepted to recover in-app instead of opening externally; login popups from healthy flows (initial sign-in, consent and step-up prompts, adding an account) are never diverted. auth.clientCertificate.pinDialog.enabled (Linux only) shows a PIN dialog for smartcard / PKCS#11 client certificates and is off by default. auth.webLogin.* pre-fills the Microsoft/federated web sign-in page so you don't retype credentials each launch: auth.webLogin.user pre-fills the email/account field, auth.webLogin.passwordCommand runs a shell command (e.g. 'pass show outlook') and pre-fills its first stdout line into the password field (the app stores no secret), auth.webLogin.autoSubmit clicks through the steps, auth.webLogin.verifyMethod clicks the matching option on the MFA 'Verify your identity' page (e.g. 'Text'), and auth.webLogin.extraHosts adds login hosts beyond the Microsoft defaults. All off/empty by default. auth.keepMsalCacheEncryptionCookie keeps the MSAL cache encryption cookie across restarts. Distinct from ssoBasicAuth*, which drive the native HTTP Basic/NTLM dialog, not the web form.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "intune.enabled",
          "type": "boolean",
          "default": false,
          "description": "Enable Single-Sign-On using Microsoft Intune."
        },
        {
          "path": "intune.user",
          "type": "string",
          "default": "",
          "description": "User (e-mail) to use for Intune SSO."
        },
        {
          "path": "webauthn.enabled",
          "type": "boolean",
          "default": false,
          "description": "Enable FIDO2 hardware security key support for WebAuthn authentication on Linux (requires fido2-tools)."
        },
        {
          "path": "webauthn.debug",
          "type": "boolean",
          "default": false,
          "description": "Enable verbose WebAuthn diagnostic logging, intended for beta testers troubleshooting key registration."
        },
        {
          "path": "webauthn.extraOrigins",
          "type": "array",
          "default": [],
          "description": "Extra sign-in origins allowed to use hardware security keys, in addition to the built-in Microsoft login origins (https://login.microsoftonline.com, https://login.microsoft.com, https://login.live.com). Add your federated IdP origin if the FIDO2 prompt happens off the Microsoft hosts. Exact https origins only: scheme, host and, where the IdP is not on 443, its port (e.g. 'https://sso.example.com' or 'https://sso.example.com:8443'). Wildcards, paths and http entries are ignored."
        },
        {
          "path": "reauthRecovery.enabled",
          "type": "boolean",
          "default": false,
          "description": "Opt into in-app recovery from stale sessions by intercepting the stale 'sign in again' login popup and recovering in-app."
        },
        {
          "path": "clientCertificate.pinDialog.enabled",
          "type": "boolean",
          "default": false,
          "description": "Show a PIN dialog for smartcard / PKCS#11 client certificates on Linux (issue #2639); off by default. Has no effect on macOS or Windows, which provide native PIN prompts."
        },
        {
          "path": "webLogin.user",
          "type": "string",
          "default": "",
          "description": "Email/username pre-filled into the account field of the web sign-in page when it is empty. Empty disables it."
        },
        {
          "path": "webLogin.passwordCommand",
          "type": "string",
          "default": "",
          "description": "Command whose first stdout line is pre-filled into the web sign-in password field. Runs in a shell; use your own password manager (e.g. 'pass show outlook'). Empty disables it. The app stores no secret."
        },
        {
          "path": "webLogin.extraHosts",
          "type": "array",
          "default": [],
          "description": "Extra hostnames (or parent domains) to treat as sign-in pages, in addition to the built-in Microsoft login hosts (login.microsoftonline.com, login.microsoft.com, login.live.com). Add your federated IdP host if sign-in happens off the Microsoft hosts."
        },
        {
          "path": "webLogin.autoSubmit",
          "type": "boolean",
          "default": false,
          "description": "Automatically advance the web sign-in: click Next after the email and Sign in after the password. Off by default so you review and submit yourself."
        },
        {
          "path": "webLogin.verifyMethod",
          "type": "string",
          "default": "",
          "description": "On the 'Verify your identity' (MFA) page, click the option whose label starts with this text, e.g. 'Text' for SMS. Empty disables it. Best-effort text match against the Microsoft method list."
        },
        {
          "path": "keepMsalCacheEncryptionCookie.enabled",
          "type": "boolean",
          "default": true,
          "description": "Sets an expiration date for the 'msal.cache.encryption' cookie to keep it after restarts (issue #2681); on by default. Set to false to keep the cookie session-scoped, at the cost of signing in again after every restart"
        },
        {
          "path": "keepMsalCacheEncryptionCookie.days",
          "type": "number",
          "default": 400,
          "description": "Sets the amount of days the 'msal.cache.encryption' cookie should be kept; defaults to 400"
        }
      ]
    },
    {
      "name": "multiAccount",
      "type": "object",
      "default": {
        "enabled": false
      },
      "description": "Multi-account profile switcher configuration (see ADR-020). enabled: opt-in flag for the single-window multi-tenant switcher. Mutually exclusive with auth.intune.enabled; when both are true a startup warning is logged and multi-account is disabled for the session.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "enabled",
          "type": "boolean",
          "default": false,
          "description": "Opt-in flag for the single-window multi-account profile switcher; mutually exclusive with auth.intune.enabled."
        }
      ]
    },
    {
      "name": "wayland",
      "type": "object",
      "default": {
        "xwaylandOptimizations": false
      },
      "description": "Wayland display server configuration. xwaylandOptimizations: keeps GPU composition enabled when running under XWayland (by default it is disabled on Wayland to prevent blank windows)",
      "applyMode": "restart",
      "fields": [
        {
          "path": "xwaylandOptimizations",
          "type": "boolean",
          "default": false,
          "description": "Keep GPU composition enabled when running under XWayland."
        }
      ]
    },
    {
      "name": "shortcuts",
      "type": "object",
      "default": {
        "global": [],
        "disableWhileFocused": []
      },
      "description": "Keyboard shortcut configuration. global: global keyboard shortcuts that work system-wide, disabled by default (opt-in). disableWhileFocused: global shortcuts to disable while the app is in focus. Replaces the deprecated globalShortcuts and disableGlobalShortcuts options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "global",
          "type": "array",
          "default": [],
          "description": "Global keyboard shortcuts that work system-wide. Disabled by default (opt-in). See configuration docs for details and limitations"
        },
        {
          "path": "disableWhileFocused",
          "type": "array",
          "default": [],
          "description": "Array of global shortcuts to disable while the app is in focus. See https://www.electronjs.org/docs/latest/api/accelerator for available accelerators to use"
        }
      ]
    },
    {
      "name": "storage",
      "type": "object",
      "default": {
        "clearData": null
      },
      "description": "Storage configuration. clearData: flag to clear storage data, expects an object of the type https://www.electronjs.org/docs/latest/api/session#sesclearstoragedataoptions. Replaces the deprecated clearStorageData option.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "clearData",
          "type": "boolean|object",
          "default": null,
          "description": "Flag to clear storage data. Expects an object of the type https://www.electronjs.org/docs/latest/api/session#sesclearstoragedataoptions"
        }
      ]
    },
    {
      "name": "urlHandling",
      "type": "object",
      "default": {
        "defaultHandler": ""
      },
      "description": "How the app hands URLs to the desktop. Replaces the deprecated defaultURLHandler option.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "defaultHandler",
          "type": "string",
          "default": "",
          "description": "Default application to be used to open the HTTP URLs"
        }
      ]
    },
    {
      "name": "appearance",
      "type": "object",
      "default": {
        "cssLocation": ""
      },
      "description": "Custom CSS configuration. Replaces the deprecated customCSSLocation option.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "cssLocation",
          "type": "string",
          "default": "",
          "description": "custom CSS styles file location"
        }
      ]
    },
    {
      "name": "platform",
      "type": "object",
      "default": {
        "chromeUserAgent": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/<version> Safari/537.36",
        "emulateWindowsChromium": false,
        "spellCheckerLanguages": []
      },
      "description": "Platform emulation and OS integration configuration. Replaces the deprecated chromeUserAgent, emulateWinChromiumPlatform and spellCheckerLanguages options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "chromeUserAgent",
          "type": "string",
          "default": "Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/<version> Safari/537.36",
          "description": "Google Chrome User Agent"
        },
        {
          "path": "emulateWindowsChromium",
          "type": "boolean",
          "default": false,
          "description": "Use windows platform information in chromium. This is helpful if MFA app does not support Linux."
        },
        {
          "path": "spellCheckerLanguages",
          "type": "array",
          "default": [],
          "description": "Array of languages to use with Electron's spell checker (experimental)"
        }
      ]
    },
    {
      "name": "app",
      "type": "object",
      "default": {
        "title": "Microsoft Outlook",
        "url": "https://outlook.office.com/mail/",
        "partition": "persist:outlook-4-linux"
      },
      "description": "Core application identity and the Outlook URL it loads. Replaces the deprecated appTitle, url and partition options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "title",
          "type": "string",
          "default": "Microsoft Outlook",
          "description": "A text to be suffixed with page title"
        },
        {
          "path": "url",
          "type": "string",
          "default": "https://outlook.office.com/mail/",
          "description": "Microsoft Outlook URL"
        },
        {
          "path": "partition",
          "type": "string",
          "default": "persist:outlook-4-linux",
          "description": "BrowserWindow webpreferences partition"
        }
      ]
    },
    {
      "name": "window",
      "type": "object",
      "default": {
        "frame": true,
        "menubar": "auto",
        "minimized": false,
        "closeOnCross": false,
        "minimizeOnClose": false,
        "alwaysOnTop": true,
        "class": null
      },
      "description": "Main window geometry, decoration and close behaviour. Replaces the deprecated frame, menubar, minimized, closeAppOnCross, minimizeOnClose, alwaysOnTop and class options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "frame",
          "type": "boolean",
          "default": true,
          "description": "Specify false to create a Frameless Window. Default is true"
        },
        {
          "path": "menubar",
          "type": "string",
          "default": "auto",
          "description": "A value controls the menu bar behaviour",
          "choices": [
            "auto",
            "visible",
            "hidden"
          ]
        },
        {
          "path": "minimized",
          "type": "boolean",
          "default": false,
          "description": "Start the application minimized"
        },
        {
          "path": "closeOnCross",
          "type": "boolean",
          "default": false,
          "description": "Close the app when clicking the close (X) cross"
        },
        {
          "path": "minimizeOnClose",
          "type": "boolean",
          "default": false,
          "description": "Minimize the window when clicking the close (X) cross instead of hiding it to the tray (ignored when closeAppOnCross is true)"
        },
        {
          "path": "alwaysOnTop",
          "type": "boolean",
          "default": true,
          "description": "Keep the pop-out window always on top of other windows."
        },
        {
          "path": "class",
          "type": "string",
          "default": null,
          "description": "A custom value for the WM_CLASS property"
        }
      ]
    },
    {
      "name": "tray",
      "type": "object",
      "default": {
        "enabled": true,
        "icon": "",
        "iconType": "default",
        "useMutationTitleLogic": true
      },
      "description": "Tray icon configuration. Replaces the deprecated trayIconEnabled, appIcon, appIconType and useMutationTitleLogic options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "enabled",
          "type": "boolean",
          "default": true,
          "description": "Enable tray icon"
        },
        {
          "path": "icon",
          "type": "string",
          "default": "",
          "description": "Custom app icon (PNG) for the tray, the window icon on Windows and Linux, and the dock on macOS. Also settable from the App Icon menu"
        },
        {
          "path": "iconType",
          "type": "string",
          "default": "default",
          "description": "Type of tray icon to be used",
          "choices": [
            "default",
            "light",
            "dark"
          ]
        },
        {
          "path": "useMutationTitleLogic",
          "type": "boolean",
          "default": true,
          "description": "Use MutationObserver to update counter from title"
        }
      ]
    },
    {
      "name": "performance",
      "type": "object",
      "default": {
        "disableGpu": false,
        "electronCLIFlags": []
      },
      "description": "GPU and Chromium startup flag configuration. Replaces the deprecated disableGpu and electronCLIFlags options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "disableGpu",
          "type": "boolean",
          "default": false,
          "description": "A flag to disable GPU and hardware acceleration (can be useful if the window remains blank)"
        },
        {
          "path": "electronCLIFlags",
          "type": "array",
          "default": [],
          "description": "Electron CLI flags"
        }
      ]
    },
    {
      "name": "development",
      "type": "object",
      "default": {
        "webDebug": false,
        "watchConfigFile": false
      },
      "description": "Debugging and development aids. Replaces the deprecated webDebug and watchConfigFile options.",
      "applyMode": "restart",
      "fields": [
        {
          "path": "webDebug",
          "type": "boolean",
          "default": false,
          "description": "Enable debug at start"
        },
        {
          "path": "watchConfigFile",
          "type": "boolean",
          "default": false,
          "description": "Watch for changes in the config file and reload the app"
        }
      ]
    }
  ]
}
